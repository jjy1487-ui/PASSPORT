# 여권 주세요 — 나중에 수정할 것 (TODO)

> 이 파일은 "지금 당장은 아니지만 나중에 손봐야 하는 것들"을 모아둔 기록입니다.
> 처리하면 `[ ]` 를 `[x]` 로 바꾸세요. (작성: 작업 세션 중 누적)
> 최종 갱신: 2026-06-07

---

## A. 게임플레이 / 데이터

- [ ] **지문 판독기(지문 스캐너) 미구현** — 3일차에 등장 예정이나 아직 기능 없음.
      윤서린(성형범죄자, day11)처럼 **지문 적발**이 정답 근거인 손님 탐지에 필요.
      (없어도 몽타주 뉴스 단서로 거절은 가능)

- [ ] **윤서린(성형범죄자) 등장 순서 변경 — 보류 중** (day11 slot1).
      앞당기려면 **뉴스(몽타주/수배 경보) 단서도 같이 그 날로 이동**해야 함(안 그러면 못 잡아서 오판 위험).
      ※ 우수사원 엔딩(누적점수 200~299)은 **점수 기반**이라 순서 바꿔도 영향 없음.

- [ ] **상점 4개 아이템의 게임플레이 효과 미구현** — 구매·돈차감은 되지만 효과 작동 X.
      `COUNSEL_BOOK`(심리상담집) / `FACE_SCANNER`(안면인식스캐너) / `CULT_DOCTRINE`(잘못된교리서) / `LONGSTAY_ID`(장기체류식별자).
      각 효과를 실제 심사/판정에 연결하는 로직 필요.

- [ ] **손님 35 이름 불일치** — 엑셀 `자오 레이` vs 에셋 파일 `지오 레이.png`.
      현재 NAME_OVERRIDE로 임시 보정 중. 둘 중 하나로 **이름 통일**(에셋 파일명 변경 또는 엑셀 name_kr 변경) 권장.

- [ ] **기존 상점 8개 아이템 처리** — 새 4개로 "교체"되며 숨겨짐(데이터 백업됨).
      기존에 작동하던 효과(X-ray권·커피=오판무효·확대경 등)도 함께 빠짐.
      나중에 통합/복구할지 결정.

## B. UI 미세조정

- [ ] **상점 카드/아이템 정렬** — 선반 6칸과 위치·크기 맞춤.
      (※ 수동 아이템 방식으로 전환 작업 진행 중 — 아래 "진행 중" 참고)

- [ ] **상점 ◀▶ 페이지네이션 미구현** — 아이템이 6개 초과될 때 필요. 지금은 그림으로만 있음.

- [ ] **상점 자금 표시 위치** — 현재 우상단 고정. 선반 디자인에 맞는 자리로 옮길 여지.

- [ ] **ResultScene 마감** — 진행 중인 것들 마무리:
      daytext("N일차 완료") 숨김/삭제 최종 결정, 반투명 글씨 패널, 버튼 아이콘(상점=상점_배경제거 / 다음날=다음날_배경제거) 적용.

## C. 정리 / 배포

- [ ] **미커밋 변경 빌드 + 커밋/푸시** — 이번 세션 작업 전체(상점 데이터·이미지·프리팹·스크립트, 비자 이름/번호, day_schedule 컬럼, 카드 Highlight 정렬, ResultScene, 배경/타이틀/아이콘 이미지 등).

- [ ] **엑셀 백업 파일 정리** — `data/여권_정리_updated.bak_*.xlsx` 여러 개 누적. 필요 없는 것 삭제.

- [ ] **day_schedule `customer_name` 컬럼** — 가독성용으로 추가됨(빌드는 무시). 커밋 대상.

---

## 진행 중 / 방금 한 것 (참고)
- 캐릭터/여권 사진 spriteRef → 한글명 정규화 (완료)
- 여권 오픈뷰 펼침 버그 수정 (완료)
- 여권 필드 대조 선택 버그 수정 (완료)
- 비자에 이름·여권번호 표시 (완료)
- 카드 Highlight를 Value에 정렬 (완료)
- 상점 데이터 4개 교체 + 아이콘 연결 + GameDatabase 반영 (완료)
- 상점 UI 수동 아이템 4개 전환 + 마우스오버 효과설명 툴팁 (완료)
- 상점 툴팁/구매 안 되던 원인 = ShopScene EventSystem 부재 → 추가 (완료)
- 상점 배경 = 밤하늘(정산화면 배경화면_0) / 패널 = 선반, 구매 시 아이콘 어둡게 (완료)
- 외국인 비자 3일차부터 요구 (이미 구현돼 있었음 — 확인 완료, 변경 불필요)
- ResultScene이 ResultSceneManager·EventSystem·카메라·버튼배선이 누락된 미완성 상태였음 → 전부 연결해 정상화 (완료)
- 상점 버튼 8일차 해금(= 7일차 완료 결과 화면부터 노출, 그 전엔 숨김) (완료)
