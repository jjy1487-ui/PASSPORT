# 여권 주세요 — 팀원 시작 가이드 (게임 내용 수정용)

> 이 문서 하나로 프로젝트를 받아 Unity로 열고, 내용을 수정해 다시 올리는 전 과정을 안내합니다.
> 게임 **실행만** 할 거면 빌드 zip(`Releases/`)을 받으면 되고, **내용을 수정**하려면 아래대로 프로젝트 전체를 받으세요.

---

## 0. 준비물 (한 번만 설치)

| 도구 | 용도 | 받는 곳 |
|---|---|---|
| **Git** | 프로젝트 받기/올리기 | https://git-scm.com |
| **Git LFS** | 큰 파일(이미지/폰트/빌드) 처리 — **필수** | https://git-lfs.com |
| **Unity Hub** | Unity 설치 관리 | https://unity.com/download |
| **Unity 6000.4.6f1** | **우리와 똑같은 버전이어야 함** | Unity Hub에서 설치 |

> ⚠️ **Git LFS는 clone 전에 반드시 설치**하세요. 안 하면 이미지·폰트가 깨진 채로 받아집니다.
> 설치 후 한 번만: `git lfs install`

---

## 1. 프로젝트 받기 (clone)

원하는 폴더에서 (예: `C:\Users\내이름\Documents`):

```bash
git lfs install
git clone https://github.com/jjy1487-ui/PASSPORT.git
cd PASSPORT
```

- 저장소 이름: **`PASSPORT`** — **공개(Public) 저장소**라 로그인·초대 없이 누구나 clone 가능합니다.
- clone하면 `PASSPORT` 폴더가 생깁니다. 기본 브랜치는 **`재영`** 이라 자동으로 `재영`을 받습니다 (모든 작업이 여기 있음).
- `git lfs install`을 먼저 했으면 clone할 때 이미지·폰트(LFS)도 자동으로 받아집니다. 혹시 깨져 보이면: `git lfs pull`
- ⛔ **GitHub의 `Download ZIP` 버튼으로 받지 마세요.** 그걸로 받으면 이미지·폰트가 실데이터 없이 "포인터(133바이트 껍데기)"로만 와서 전부 깨집니다. **반드시 `git clone`** 으로 받으세요.

> 📌 가장 안전한 방법: GitHub 저장소 페이지에서 초록색 **`Code` 버튼 → HTTPS 주소 복사** 해서 clone. 항상 정확합니다.

---

## 2. Unity로 열기

1. **Unity Hub** 실행 → `Add` → `Add project from disk` → clone로 받은 `PASSPORT` 폴더 선택
2. Unity 버전이 **6000.4.6f1** 인지 확인 (다르면 Hub에서 그 버전 설치 후 열기)
3. 처음 열면 임포트에 몇 분 걸립니다 (정상)
4. 열리면 `Assets/Scenes/ImmigrationScene.unity` 가 메인 플레이 씬입니다. 더블클릭해서 열고 ▶ 누르면 1일차부터 플레이됩니다.

---

## 3. 게임 내용이 어디에 있나 (수정 지점)

| 고치려는 것 | 위치 / 방법 |
|---|---|
| **대사·점수·손님·금액·비자·서류 등 데이터** | 저장소 안 엑셀 `data/여권_정리_updated.xlsx`(21시트) 수정 → 아래 4번 임포트 |
| **화면 배치·UI** | `Assets/Scenes/ImmigrationScene.unity` 씬에서 편집 |
| **게임 규칙·로직** | `Assets/Scripts/` (Manager·Inspection·Data 폴더) |
| **데이터 변환 스크립트** | `Tools/DataImport/` (python) |

> 데이터(밸런스·대사)는 **엑셀만 고치고 재임포트**하면 코드 수정 없이 반영됩니다.

---

## 4. 데이터(엑셀) 수정 → 게임 반영 절차

1. 저장소 안 엑셀 `data/여권_정리_updated.xlsx`(21시트, 1행 PK / 2행 자료형 / 3행 영문컬럼 / 4행 한글 / 5행~데이터)에서 값 수정 — **이게 단일 소스**(파이프라인이 저장소 상대경로로 이 파일을 읽음, PC 독립)
2. 변환: `Tools/DataImport/` 에서 `python xlsx_to_json.py` 실행 → `Assets/GameData/_source/GameData.source.json` 갱신
3. Unity 메뉴 **`Tools > Passport > Import Data`** 실행 → `Assets/GameData/*.asset` 테이블 갱신
4. (per-day 데이터까지 바꾸려면) `python build_days.py` 로 `Assets/Resources/GameData/dayN.json` 재생성
5. ▶ 플레이로 확인

---

## 5. 빌드 만들기 (실행파일)

- Unity 메뉴 또는 빌드 설정에서 **`ImmigrationScene`만** 포함해 Windows 빌드
- 빌드 결과의 `*_BurstDebugInformation_DoNotShip` 폴더는 배포에서 제외
- 빌드 산출물(`Build/`)은 git에 안 올라갑니다(.gitignore). 배포는 zip → `Releases/` 또는 GitHub Release 사용

---

## 6. 협업 규칙 (브랜치 워크플로) ⭐ 중요

**여러 명이 같이 작업하므로, `재영`에 바로 커밋하지 말고 개인 브랜치에서 작업 → 합치기(PR)** 합니다.

```bash
# 1) 항상 최신 재영에서 시작
git checkout 재영
git pull

# 2) 내 작업용 브랜치 만들기 (이름은 작업 내용으로)
git checkout -b 대사수정-내이름

# 3) Unity에서 내용 수정 후, 바뀐 것 올리기
git add -A
git commit -m "5~7일차 대사 보강"
git push -u origin 대사수정-내이름

# 4) GitHub 웹에서 'Pull Request' 만들어 재영으로 합치기 요청
```

### ⚠️ Unity 협업 주의 (꼭 지키기)
- **같은 씬(.unity)·프리팹을 두 명이 동시에 편집하지 마세요.** Unity 씬은 충돌 해결이 매우 어렵습니다. 누가 씬을 만질지 미리 정하세요.
- **Git LFS 설치 필수** (이미지·폰트·빌드).
- **Library/, Temp/, Build/ 폴더는 올리지 않습니다** (이미 .gitignore 처리됨).
- Unity 버전 **6000.4.6f1** 통일.
- `.unity` 씬 파일을 **Unity 밖에서 직접 편집 금지** (에디터가 멈출 수 있음).

---

## 7. 진행 상황 문서

- `PROGRESS.md` — 지금까지 한 작업·다음 할 일이 다 정리돼 있습니다. **새로 시작할 땐 이걸 먼저 읽으세요.**
- `Tools/DataImport/BRANCH_CATALOG.md` — 캐릭터별 점수/금액/분기 명세

---

## 8. 자주 막히는 것

| 증상 | 해결 |
|---|---|
| 이미지·폰트가 깨져 보임 | Git LFS 미설치 → `git lfs install` 후 `git lfs pull` |
| "버전이 다르다" 경고 | Unity 6000.4.6f1로 열기 |
| 한글이 두부(□)로 깨짐 | malgun 폰트 사용 (코드에서 FindFont로 처리됨) |
| 데이터 바꿨는데 반영 안 됨 | `Tools > Passport > Import Data` 재실행했는지 확인 |

---

## 9. 관리자(저장소 주인)가 할 일 — GitHub 웹에서

> `gh` CLI가 없으므로 아래는 GitHub 웹사이트에서 직접:

1. **팀원 초대**: 저장소 → `Settings` → `Collaborators` → `Add people` (이미 초대 완료)
2. (선택) **브랜치 보호**: `Settings` → `Branches` → `재영`에 "PR 거쳐서만 합치기" 규칙을 걸면 실수로 덮어쓰는 걸 막습니다.
3. (선택) **빌드 배포**: `Releases` → `Draft a new release` → `Releases/PassportPlease_Day1-14_Win64.zip` 첨부 → 팀원에게 링크 전달
